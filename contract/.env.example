# Copy to contract/.env and fill in values
# Never commit your real secrets

# Sepolia RPC endpoint (Alchemy/Infura/QuickNode/etc.)
SEPOLIA_RPC_URL=

# Private key of the deployer wallet (no 0x prefix or with 0x is fine for Hardhat)
# Use a dedicated testnet account only
PRIVATE_KEY=

# Optional: Etherscan API key for contract verification
ETHERSCAN_API_KEY=
